{% extends 'layout' %}
{% block body %}
<div class="content">
	 <div class="top_bar">Bartendro<div class="logo">by Party Robotics</div></div>
	 
    <table class="home_table">
    {% for drink in drinks %}
      {% if (loop.index0 % 2) == 0 %}
         <tr> 
      {% endif %}
      <td class="drink" >
      	<a href="/drink/{{ drink.id }}">
      		<div class="sub">
      			<div class="drink_name">{{ drink.name.name }}</div>
      			</br> 
	     			<div class="ingredients">{{ drink.desc }}</div>
	     			<div class="drink_desc">here is some fancy bullshit explaination of why you want this drink</div>
      		</div>
      	<a>
      </td>
     
      {% if (loop.index0 % 2) == 1 %}
         </tr> 
      {% endif %}
    {% endfor %}
    </table>
</div>
<div>
{% endblock %}
